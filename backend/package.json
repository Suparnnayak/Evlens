{
  "name": "event-review-summarizer-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Event Review Summarizer",
  "engines": {
    "python": "3.12.0"
  },
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}

